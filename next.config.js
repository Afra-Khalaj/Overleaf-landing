/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      defaultLocale: 'fa',
      locales: ['fa', 'en'],
      // localeDetection: true,
    },
  };
  
  module.exports = nextConfig;